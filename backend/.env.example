# Local backend only (no Docker). Copy from repo root instead:
#   cp ../.env.example .env
#
# Docker uses root .env (copy from ../.env.dev or ../.env.prod). backend/.env is
# not used inside containers — safe to omit when running via docker compose.
